# next-auth (beta) declares an OPTIONAL peer on @simplewebauthn/browser@^9, but we
# run @simplewebauthn v13 with our own WebAuthn flow (not next-auth's provider). The
# peer is optional and unused, so relax strict peer resolution to keep `npm ci`
# (CI + Docker build) consistent with `npm install`.
legacy-peer-deps=true
